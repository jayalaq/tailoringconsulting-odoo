# Setting Rules Payroll

## Summary

This module has been designed with the purpose of extending the configuration actions in the salary rules model (hr.salary.rule), so it creates a tab called "configuration".
